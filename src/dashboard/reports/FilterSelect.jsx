// Re-export shim — source of truth lives in src/components/reports/.
// SearchFilter has its own file (../SearchFilter) so this stays default-only.
export { default } from '../../components/reports/FilterSelect';
