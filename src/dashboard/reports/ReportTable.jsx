// Re-export shim — the source of truth now lives in src/components/reports/.
// Kept here so distributor view files don't need their imports updated.
export { default } from '../../components/reports/ReportTable';
